{
  "name": "@memo-inbox/shared-types",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "pnpm run typecheck",
    "test": "vitest run src/__tests__"
  }
}
